#!/usr/bin/env bash

if [ $# -ne 2 ]; then
    echo "Usage: $0 <input_file> <output_file>"
    exit 1
fi

input_file="$1"
output_file="$2"

ffmpeg -i "$input_file" -f rawvideo -pixel_format yuv420p - > "$output_file"
